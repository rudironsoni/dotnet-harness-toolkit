name: RuleSync Validate

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci

      - name: Validate RuleSync config (dry run)
        run: npx rulesync generate --dry-run

      - name: Install declarative sources
        run: npx rulesync install --frozen || npx rulesync install

      - name: Generate target files
        run: npx rulesync generate
